<table class="table">
        <thead>
          <tr>
            <th scope="col">Category Name</th>
            <th scope="col">Markers</th>
          </tr>
        </thead>
        <tbody id="tbody">
        
          
            <% @categories.each do |category|%>
            <tr>
              <td><%= category.name%></td>
              <td>
                <ul>
                <% marker_for_category(category).each do |m| %>
                <div id="list">
                  <li id='mark-<%=m.marker.id%>'><a href="<% m.marker.url%>"><%=m.marker.title + '         |'%>  </a>
                
                  <%= link_to 'show', m.marker, remote: true %>
                  <%= link_to 'edit', edit_marker_path(m), remote: true %>
                  <%= link_to 'destroy', marker_path(m), data: {remote: true, confirm: 'Are you sure?', method: :delete} %>
                </li>
                
                </div>
                
                <%end%>
                </ul>
              </td>
            </tr>
            <%end%>
    
        </tbody>
      </table>
    